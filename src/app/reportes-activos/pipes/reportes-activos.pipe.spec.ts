import { ReportesActivosPipe } from './reportes-activos.pipe';

describe('ReportesActivosPipe', () => {
  it('create an instance', () => {
    const pipe = new ReportesActivosPipe();
    expect(pipe).toBeTruthy();
  });
});
